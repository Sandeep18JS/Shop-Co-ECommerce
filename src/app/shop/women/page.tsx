import Filters from "@/components/Filters";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Filters />
    </div>
  );
};

export default page;
